clear all
close all

h = .1;
time = 0:h:100;


U = 1.5;
omega = 0;
x = zeros(time);
y = zeros(time);

x(1) = 100;
y(1) = 0;

for t = time(2:end)
    dx = U*cos(omega*t);
    dy = U*sin(omega*t);
    
    
    x(t) = x(t-1) + dx;
    x(t) = x(t-1) + dx;
    
end
    